export type INavLink = {
  imgURL: string;
  route: string;
  label: string;
  icon: any;
};

export type IUser = {
  id: string;
  fullname: string;
  username: string;
  email: string;
  imageUrl: string;
  bio: string;
};
